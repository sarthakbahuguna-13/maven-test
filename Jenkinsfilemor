Thank you for the clarification.

To clarify, our current blocker is not repository-level access. The configuration is set up to run through Terraform Cloud using a private provider, and we currently have only Read access to the relevant workspace.

Since the execution is restricted to Terraform Cloud remote runs, we require Plan/Run (or Write) access to the Non-Prod workspace to proceed with testing and validation.

Could you please help grant the appropriate access to the Non-Prod workspace?

node {
def mavenHome = tool name: "maven-1"

echo "the node name is : --> ${env.NODE_NAME}"

echo "the job name is : ---> ${env.JOB_NAME}"

echo "this build number is: --> ${env.BUILD_NUMBER}"

properties([buildDiscarder(logRotator(artifactDaysToKeepStr: '', artifactNumToKeepStr: '', daysToKeepStr: '', numToKeepStr: '5'))])

stage('checkout'){

git 'https://github.com/sanchitraj35/maven-test.git'
}

//build stage
/*
stage('build'){

sh "$mavenHome/bin/mvn clean package"
}
//deploy stage
stage('deploy'){
sshagent([]) {
 sh "scp -o StrictHostKeyChecking=no target/maven-web-application.war ec2-user@3.235.225.165:/opt/apache-tomcat-11.0.14/webapps" 
}
}
*/
}
